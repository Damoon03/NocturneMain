//
//  AppColors.swift
//  Nocturne
//
//  Shared color constants. Sheets/popups use a navy-tinted black so they
//  read as distinct surfaces from the pure black used by the app's main
//  screens (Library, Song editor) — not literal black-on-black.
//

import SwiftUI

extension Color {
    /// Delete confirmations, Recordings, Fragments, Folders, Profile, etc).
    static let nocturneSheetBackground = Color(red: 0.0 / 255.0, green: 0.0 / 255.0, blue: 0.0 / 255.0)
}
